# Rust build artifacts
/target
**/*.rs.bk
*.profraw
rustc-ice-*.txt

# Built notification-helper bundle (assembled by scripts/triage-notify/build.sh
# from the tracked main.swift + Info.plist; the output bundle isn't checked in)
scripts/triage-notify/triage-notify.app/

# macOS
.DS_Store
._*

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log

# Environment files
.env
.env.local
.env.*.local

# AI agent local config — per-machine, never committed
.claude/*.local.*
.claude/settings.local.json
.codex/*.local.*
.cursor/*.local.*
.aider.local.*
